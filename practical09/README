This is a code file for Practical 9. It performs the operation of loading a text file and checking if the matrix is a magic square or not.

1. main.c
2. magic_square.h (Contains the function isMagicSquare)

Text files
1. magic_square.txt
2. not_magic_square.txt

To compile

gcc -o magic_square main.c -lm -Wall

Input the text files as filenames to the function, one by one.

